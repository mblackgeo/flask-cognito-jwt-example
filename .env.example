# -----------------
# Required settings
# -----------------
FLASK_APP=webapp
FLASK_ENV=dev
FLASK_SITE_URL=http://localhost:5000

# AWS account and region used where infrastructure is deployed
# CDK requires these to know where to deploy resources
AWS_ACCOUNT=
AWS_REGION=

# These are required if running locally. The values can be obtained from the AWS
# console after deploying.
# When the app is deployed with CDK these values are automatically populated in
# the in the Lambda environment.
AWS_COGNITO_DOMAIN=
AWS_COGNITO_USER_POOL_ID=
AWS_COGNITO_USER_POOL_CLIENT_ID=
AWS_COGNITO_USER_POOL_CLIENT_SECRET=

# -----------------
# Optional settings
# -----------------
# optional, set to deploy to a custom URL: <api_subdomain.route53_domain_name>
# Must have the domain registered in Route 53
AWS_DOMAIN_NAME=
AWS_API_SUBDOMAIN=

# Set an optional domain for Cognito authentication. If blank, one will be
# generated by Cognito (if set, requires AWS_DOMAIN_NAME as well)
AWS_COGNITO_SUBDOMAIN=